import { View, Text } from "react-native";

export default function RoleSelect() {
  return (
    <View
      style={{
        flex:1,
        justifyContent:"center",
        alignItems:"center"
      }}
    >
      <Text>Role Selection Screen</Text>
    </View>
  );
}